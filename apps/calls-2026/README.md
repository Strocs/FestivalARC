# Temporary Festival ARC 2026 calls

This is a temporary `kind: calls` application. It may publish at the root during the call period, but it has no archive destination. Final Festival ARC 2026 publication replaces and removes this app rather than archiving its routes or data.
